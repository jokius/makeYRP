import palette from '../../../../shared/helpers/palette'

const styles = () => ({
  menuBody: {
    position: 'fixed',
    zIndex: 1000,
    top: 55,
    height: '100vh',
    width: 310,
    backgroundColor: palette.white,
  },
})

export default styles
