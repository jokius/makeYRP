import palette from '../../../../shared/helpers/palette'

const styles = () => ({
  root: {
    backgroundColor: palette.defaultBg,
    height: 55,
  },

  selectMenu: {
    backgroundColor: palette.gray52,
  },
})

export default styles
