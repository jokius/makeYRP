const styles = (theme) => ({
  root: {
    position: 'fixed',
    top: '50%',
    left: '50%',
  },

  closeButton: {
    position: 'absolute',
    right: theme.spacing(1),
    top: theme.spacing(1),
    color: theme.palette.grey[500],
  },
})

export default styles
